# Chapter02-03-image-filter

## 준비
```bash
pip install -r requirements.txt
```
- `images/example.png`가 있어야 미리보기 섹션이 표시됩니다.

## 실행
```bash
streamlit run app.py
```
- 이미지 업로드 후 필터(Grayscale, Color Intensity, Blur, RGB 조정)를 선택해 결과를 확인합니다.

## 종료
- 터미널에서 `Ctrl + C`로 서버를 중지합니다.

