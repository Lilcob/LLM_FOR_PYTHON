# Chapter02-02-bmi-calculator

## 준비
```bash
pip install -r requirements.txt
```

## 실행
```bash
streamlit run 01_BMI_calc.py.py
```
- 사이드바에서 BMI 공식과 기준표를 확인하고, 입력값 단위(Metric/Imperial)를 선택합니다.
- 개별 BMI 계산과 CSV 업로드(`height`, `weight` 컬럼) 두 가지 방식을 지원합니다.

## 종료
- 터미널에서 `Ctrl + C`로 서버를 중지합니다.

