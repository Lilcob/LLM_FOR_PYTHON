# Chapter02-03-paperHelper

## 준비
```bash
pip install -r requirements.txt
```
- `.env`에 `OPENAI_API_KEY`를 설정합니다.

## 실행
```bash
streamlit run main.py
```
- PDF를 업로드하면 페이지별 텍스트를 추출해 요약/분석 작업을 수행합니다.
- 사이드바에서 모델, 출력 길이, 심층도, 언어 등을 선택한 뒤 실행합니다.

## 종료
- 터미널에서 `Ctrl + C`로 서버를 중지합니다.

