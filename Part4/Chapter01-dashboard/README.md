# Chapter01-dashboard

## 준비
```bash
pip install streamlit requests
```

## 실행
```bash
streamlit run app.py
```
- 캘린더, 날씨(24시간 예보), USD→KRW 환율, IT 뉴스 헤드라인을 한 화면에서 확인합니다.
- 개별 예제는 `calendar.py`, `weather.py`, `exchange.py`, `news.py`로 분리되어 있습니다.

## 종료
- 터미널에서 `Ctrl + C`로 서버를 중지합니다.

